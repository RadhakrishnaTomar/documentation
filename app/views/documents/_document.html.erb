<div class="bg-white shadow rounded-lg p-6 mb-6">
  <h2 class="text-xl font-semibold text-indigo-700 mb-2">📄 <%= document.title %></h2>

  <p class="text-gray-700 mb-1">
    <strong>Category:</strong> <%= document.category.presence || "Not categorized" %>
  </p>

  <% if document.file.attached? %>
    <p class="text-gray-700">
      <strong>File:</strong>
      <%= link_to "⬇️ Download", rails_blob_path(document.file, disposition: "attachment"), class: "text-blue-600 hover:underline" %>
    </p>
  <% else %>
    <p class="text-red-600 font-medium">⚠️ No file attached</p>
  <% end %>
</div>
